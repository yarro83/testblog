- if comment.persisted?
  .comment
    .comment__header
      %strong= comment.user.to_s
      = distance_of_time_in_words_to_now(comment.created_at)
      ago
    .comment__body
      = comment.body

